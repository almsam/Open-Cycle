import React from 'react';

const Calendar: React.FC = () => {
    return (
        <div className="calendar-container">
            <h2 className="text-xl font-bold">Cycle Tracker Calendar</h2>
            {/* Calendar implementation will go here */}
        </div>
    );
};

export default Calendar;